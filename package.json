{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build && sed -i 's|goku|kau/goku|' .vercel/output/config.json && cat .vercel/output/config.json",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.0"
  }
}
